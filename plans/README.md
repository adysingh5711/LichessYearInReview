# Implementation Plans

Generated by the improve skill on 2026-07-11, against commit `a93da87`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Verification baseline (CI, typecheck, lint rules, single lockfile) | P1 | S | — | DONE |
| 002  | Characterization tests for analyzer + PGN parser | P1 | M | 001 | DONE |
| 003  | Fix OG image pipeline (retire puppeteer, wire /api/og) | P1 | S | 001 | TODO |
| 004  | Case-insensitive username matching | P1 | S | 002 | TODO |
| 005  | Stats-math bug batch (aborted games, NaN ratings, dates) | P1 | M | 002, 004 | TODO |
| 006  | API input hardening (upload limits, validation, safe errors) | P2 | M | 005 | TODO |
| 007  | Dependency cleanup (one animation lib, drop dead packages) | P2 | S | 001 | TODO |
| 008  | Dead code removal + quick UI perf fixes | P2 | S | 005, 006 | TODO |
| 009  | Fast PGN parsing (drop chess.js move replay) | P2 | M | 002, 005 | TODO |
| 010  | Split chess-analyzer god component | P3 | L | 008 | TODO |
| 011  | README rewrite + retire lyir.py | P3 | S | — (best after 003, 009) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first, always** — it creates `pnpm typecheck` and the CI gate every other plan uses for verification, pins pnpm, and fixes the one pre-existing type error.
- **002 before 004/005/009** — the characterization suite is the regression net for every behavior change to `lib/analyzer.ts` and `lib/pgn-parser.ts`. Plans 004/005 flip specific `KNOWN BUG`-marked assertions; 009 must keep the rest green untouched.
- **005 before 006 before 008** — all three edit `app/api/fetch-games/route.ts` and/or `components/chess-analyzer.tsx`; this ordering avoids merge conflicts and each plan's drift check expects its predecessors' diffs.
- **008 before 010** — delete dead code before extracting components.
- **003 and 007 are independent** — can run in parallel with the 002→005 chain (003 and 007 both touch `package.json`; land them at different times or rebase).
- **009 removes `chess.js`** — 007 deliberately leaves `chess.js`/`@types/chess.js` for 009 to finish.

## Findings considered and rejected

- **`.env.example` / env-var docs**: the app has zero runtime env vars (`grep process.env` across app/lib/components is empty). Nothing to document.
- **Analyzer algorithmic performance**: `lib/analyzer.ts` is a clean single-pass aggregation with keyed lookups — no N+1 or complexity problem. (The real cost was chess.js move replay in the parser → plan 009.)
- **Fixing `lyir.py`'s crash bugs** (non-standard time controls, zero-division): it duplicates the TS analyzer and is retired to `scripts/legacy/` by plan 011 instead.
- **`CLAUDE.md`/`AGENTS.md`**: repo is small; low leverage unless agent-driven work becomes routine.
- **Rate limiting / auth on API routes**: worthwhile but needs an infra decision (store/Vercel config); noted in plan 006's maintenance section rather than planned.
- **Streaming the Lichess NDJSON fetch**: belongs to the "productize direct fetch" direction spike (not selected this round); plan 006 caps `max=10000` as the stopgap.

## Direction options (surfaced, not selected this round)

Recorded for a future `next`-focused run: productize the direct-fetch flow (pagination + progress streaming), personalized `@vercel/og` share card (extends plan 003), opening study recommendations from existing per-opening stats, badges/milestones from existing streak/peak data.
